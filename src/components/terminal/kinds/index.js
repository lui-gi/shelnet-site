// Maps a module's `kind` to its runner. Add new kinds here as they are built.
import * as guidedWalkthrough from './guidedWalkthrough';

export const RUNNERS = {
  'guided-walkthrough': guidedWalkthrough,
};
