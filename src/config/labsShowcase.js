// src/config/labsShowcase.js
// Labs is being rebuilt as a self-contained showcase. Until it ships, this is
// the single source of truth for the "coming" labs, also used for the hero
// labs count (see resourceCounts.js).
export const COMING_LABS = [
  'custom SOC',
  'mock corporate virtual network',
  'splunk + edr builds',
];
